/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['aceternity.com', 'encrypted-tbn0.gstatic.com', 'seeklogo.com', 'download.logo.wine', 'w7.pngwing.com', 'upload.wikimedia.org'],
    },
};

export default nextConfig;
